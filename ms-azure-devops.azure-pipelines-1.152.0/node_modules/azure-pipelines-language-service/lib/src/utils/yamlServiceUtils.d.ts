import { TextDocument, Position } from "vscode-languageserver-types";
export declare const nodeHolder = "~";
export interface CompletionAdjustment {
    newText: string;
    newPosition: Position;
}
export declare function completionHelper(document: TextDocument, textDocumentPosition: Position): CompletionAdjustment;
